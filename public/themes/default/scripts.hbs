<script>
$(document).ready(function(){
     var targetText=$("#weixinCode").text();
     var clipboard = new Clipboard('#copy_btn');
     clipboard.on('success', function(e) {
           // console.info('Action:', e.action);
           // console.info('Text:', e.text);
           // console.info('Trigger:', e.trigger);
           // alert("复制成功");
           console.log('复制成功', targetText);
           e.trigger.value = '请打开微信加好友';
           e.clearSelection();
            setTimeout(function() {
              e.trigger.value = '点击复制微信号';
            }, 3000);
     });
     var getPriceBtn = new Clipboard('.get_price_btn');
      getPriceBtn.on('success', function(e) {
           console.log('复制成功', targetText);
           e.trigger.value = '请微信添加好友';
           e.clearSelection();
            setTimeout(function() {
              e.trigger.value = '点击复制微信号';
            }, 3000);
      });
 });
</script>